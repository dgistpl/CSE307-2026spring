(* CSE307 Spring 2026 - Homework 1, Problem 11 *)
(* Name: *)
(* Student ID: *)

type nat = ZERO | SUCC of nat

(* natadd : nat -> nat -> nat *)
(* Add two natural numbers. *)
let rec natadd (n1 : nat) (n2 : nat) : nat =
  failwith "TODO"

(* natmul : nat -> nat -> nat *)
(* Multiply two natural numbers. *)
let rec natmul (n1 : nat) (n2 : nat) : nat =
  failwith "TODO"
